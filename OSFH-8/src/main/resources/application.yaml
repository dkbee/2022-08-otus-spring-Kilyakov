spring:
  output:
    ansi:
      enabled: ALWAYS
  data:
    mongodb:
      database: library
      port: 27017
      host: localhost

mongock:
  #runner-type: "ApplicationRunner" # default
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.spring.kilyakov.mongock.changelog